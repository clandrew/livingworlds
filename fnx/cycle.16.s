    ; 141-145 inclusive
    LDA >#(LUT_START + (141*4))
    STA src_pointer+1
    LDA <#(LUT_START + (141*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 146-150 inclusive
    LDA >#(LUT_START + (146*4))
    STA src_pointer+1
    LDA <#(LUT_START + (146*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 151-155 inclusive
    LDA >#(LUT_START + (151*4))
    STA src_pointer+1
    LDA <#(LUT_START + (151*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 156-160 inclusive
    LDA >#(LUT_START + (156*4))
    STA src_pointer+1
    LDA <#(LUT_START + (156*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 161-165 inclusive
    LDA >#(LUT_START + (161*4))
    STA src_pointer+1
    LDA <#(LUT_START + (161*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 166-170 inclusive
    LDA >#(LUT_START + (166*4))
    STA src_pointer+1
    LDA <#(LUT_START + (166*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 171-175 inclusive
    LDA >#(LUT_START + (171*4))
    STA src_pointer+1
    LDA <#(LUT_START + (171*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 190-194 inclusive
    LDA >#(LUT_START + (190*4))
    STA src_pointer+1
    LDA <#(LUT_START + (190*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 195-199 inclusive
    LDA >#(LUT_START + (195*4))
    STA src_pointer+1
    LDA <#(LUT_START + (195*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 200-204 inclusive
    LDA >#(LUT_START + (200*4))
    STA src_pointer+1
    LDA <#(LUT_START + (200*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 205-209 inclusive
    LDA >#(LUT_START + (205*4))
    STA src_pointer+1
    LDA <#(LUT_START + (205*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 210-214 inclusive
    LDA >#(LUT_START + (210*4))
    STA src_pointer+1
    LDA <#(LUT_START + (210*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

    ; 215-219 inclusive
    LDA >#(LUT_START + (215*4))
    STA src_pointer+1
    LDA <#(LUT_START + (215*4))
    STA src_pointer
    LDA #4; Cycle length
    JSR CycleColors

